📂 Структура проекта
avito_test/
├── api/
│   ├── base_api.py
│   └── item_api.py
│
├── data/
│   └── item_data.py
│
├── tests/
│   ├── test_item_create.py
│   ├── test_item_get.py
│   ├── test_item_get_by_seller.py
│   ├── test_item_delete.py
│   └── conftest.py
│
├── allure-results/
│
├── pytest.ini
├── requirements.txt
└── README.md

🔹 api/
Слой работы с API.
Содержит классы-обёртки над HTTP-запросами, чтобы тесты не работали напрямую с requests.
base_api.py
Базовый класс для всех API:
хранит базовый URL
реализует методы get, post, delete
централизует работу с HTTP
item_api.py
API-клиент для работы с объявлениями (Item):
создание объявления
получение объявления по ID
получение списка объявлений продавца
удаление объявления
Используется напрямую в тестах через фикстуру item_api.

🔹 data/
Хранилище тестовых данных и наборов входных значений.
item_data.py
VALID_ITEM_BASE — базовый валидный payload объявления
INVALID_ITEM_PAYLOADS — набор невалидных payload’ов для негативных тестов
INVALID_ITEM_IDS / INVALID_SELLER_IDS — некорректные ID для параметризации тестов
Позволяет:
не дублировать данные в тестах
легко расширять негативные сценарии

🔹 tests/
Автотесты API, сгруппированные по функциональности.

test_item_create.py
Тесты создания объявления:
успешное создание
проверки валидации (400 Bad Request)

test_item_get.py
Тесты получения объявления по ID:
успешный сценарий
несуществующий ID
некорректный ID

test_item_get_by_seller.py
Тесты получения объявлений по sellerId:
один объект
несколько объектов
отсутствие объявлений
невалидный sellerId

test_item_delete.py
Тесты удаления объявления:
успешное удаление
повторное удаление
удаление несуществующего объявления
(часть тестов выявляет баги API)

conftest.py
Общие фикстуры:
item_api — инициализация API-клиента
valid_item_payload — генерация валидного payload
created_item — создание тестового объявления перед тестами